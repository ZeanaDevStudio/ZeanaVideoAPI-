# Zeana Video API

This is the backend API for the Zeana app. It allows:

- Text/Image → Video generation (using a placeholder image + audio)
- Video returned as MP4
- Fully functional free API for testing and app integration

## Usage

POST request to:
